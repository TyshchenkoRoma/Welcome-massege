machine:
  java:
    version: openjdk8
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/mesTests/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/mesTests/ \;

